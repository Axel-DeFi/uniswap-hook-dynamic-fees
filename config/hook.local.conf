# Hook deployment config (dotenv-style KEY=VALUE)
# Contour: local (Anvil fork of Ethereum Sepolia)
# Used by: ./test/scripts/test_run.sh local <fast|full>

# Local Anvil RPC (where the runner will spawn anvil)
RPC_URL=http://127.0.0.1:8545

# Fork source (Sepolia RPC) — public, no API key required
FORK_URL=https://ethereum-sepolia-rpc.publicnode.com

# Uniswap v4 PoolManager on Sepolia
POOL_MANAGER=0xE03A1074c86CFeDd5C142C4F04F1a1536e203543

# Pool binding (explicit roles)
VOLATILE=0x7b79995e5f793A07Bc00c21412e50Ecae098E7f9
STABLE=0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238
STABLE_DECIMALS=6
TICK_SPACING=10

# Init (human price: STABLE per 1 VOLATILE)
INIT_PRICE_USD=3000

# Guardian (local): Anvil account #0 address (default)
GUARDIAN=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266

# Fee model params (indices refer to contract's fee buckets)
INITIAL_FEE_IDX=1
FLOOR_IDX=1
CAP_IDX=4

# Timing / smoothing
PERIOD_SECONDS=300
EMA_PERIODS=12
DEADBAND_BPS=1000
LULL_RESET_SECONDS=3600

# Pause mode
PAUSE_FEE_IDX=2

# Compatibility aliases (keep for existing scripts)
TOKEN_A=${VOLATILE}
TOKEN_B=${STABLE}
